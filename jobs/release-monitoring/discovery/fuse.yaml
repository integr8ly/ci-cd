---
- job:
    name: release-monitoring-discovery/fuse
    display-name: 'Fuse'
    description: "Monitoring <a href=\"https://github.com/jboss-fuse/application-templates/tags\">https://github.com/jboss-fuse/application-templates/tags</a> (Latest containing redhat) for latest GA release.<br/>
    Next Branch: <a href=\"https://github.com/integr8ly/installation/tree/fuse-next\">https://github.com/integr8ly/installation/tree/fuse-next</a> is being automatically tested by this <a href=\"/view/Product%20Release%20Monitoring/job/release-monitoring-branch/job/fuse-next/\">next branch job</a>"
    project-type: pipeline
    concurrent: false
    triggers:
      - timed: '@hourly'
    parameters:
      - string:
          name: 'manifestVar'
          default: 'fuse_release_tag'
          description: '[REQUIRED] The manifest variable to be used as the current component version'
          read-only: true
      - string:
          name: 'projectOrg'
          default: 'jboss-fuse'
          description: '[REQUIRED] github project organization'
          read-only: true
      - string:
          name: 'projectRepo'
          default: 'application-templates'
          description: '[REQUIRED] github project repository'
          read-only: true
      - string:
          name: 'productName'
          default: 'fuse'
          description: '[REQUIRED] Product to check, this affects the way the job verifies if a new version is available'
          read-only: true
      - string:
          name: 'releaseFetchMethod'
          default: 'tag'
          description: '[REQUIRED] Method to fetch latest release which can either be by tag or by release'
          read-only: true
      - string:
          name: 'gaReleaseTagRef'
          default: 'redhat'
          description: '[REQUIRED] Reference used to filter GA releases from the repository tags'
          read-only: true
    pipeline-scm:
      script-path: jobs/release-monitoring/discovery/github/Jenkinsfile
      scm:
        - git:
            branches:
              - master
            url: 'https://github.com/integr8ly/ci-cd.git'
            skip-tag: true
            wipe-workspace: false
